# Local secrets for Docker / database
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
POSTGRES_DB=appdb

# Optional Docker registry credentials (for local development only - do not commit actual values)
# For CI, use GitHub Secrets instead
DOCKER_USERNAME=your_docker_user
DOCKER_PASSWORD=your_docker_password
