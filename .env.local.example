# Local secrets for Docker / database
POSTGRES_USER=app
POSTGRES_PASSWORD=change-me
POSTGRES_DB=appdb

# Optional Docker registry credentials (do not commit actual values)
DOCKER_USERNAME=your_docker_user
DOCKER_PASSWORD=your_docker_password
